[project]
name = "tiny-transformer-summarizer"
version = "0.1.0"
description = "A small Transformer summarizer for learning Encoder-Decoder Transformers."
requires-python = ">=3.10"
dependencies = [
    "datasets>=3.0.0,<4.0.0",
    "fastapi>=0.136.3",
    "torch>=2.3.0",
    "tqdm>=4.66.0",
    "uvicorn[standard]>=0.49.0",
]

[tool.uv]
package = false
